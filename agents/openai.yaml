interface:
  display_name: "DOCX Paper Formatter"
  short_description: "Format papers with reusable Word styles, four-level heading numbering, abstract pagination, directory removal, and reference numbering."
  default_prompt: "Use this skill when a user needs a paper DOCX normalized against a teacher formatting requirement DOCX, extracting layout and heading-level rules from teacher text or tables when possible, optionally saving them as reusable validated JSON config plus schema, and applying reusable Word styles, four-level multilevel heading numbering, abstract pagination, directory removal, reference auto-numbering, page setup, and a format-check report."
